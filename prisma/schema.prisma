generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Conversation {
  id          Int      @id @default(autoincrement())
  userId      String
  userMessage String
  aiMessage   String
  createdAt   DateTime @default(now())
}
